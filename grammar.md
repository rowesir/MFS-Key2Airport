# Key2Airport 配置文件语法

本文档定义 Key2Airport 配置文件的正式格式和执行规则。

配置文件使用**标准 JSON**，文件扩展名建议使用 `.json`。

标准 JSON 不支持 `//` 或 `/* */` 注释，因此正式配置文件中不能出现注释。`grammartest.txt` 仅用于讨论格式，里面的注释和示例不能直接作为正式配置文件使用。

---

## 1. 顶层结构

配置文件的根节点必须是 JSON 对象，并且必须包含以下 6 个字段：

```text
AIRCARFT
RA_CHECKBOX
LD_CHECKBOX
PAGESWITCH
PAGE_1
PAGE_2
```

字段名称必须完全匹配，大小写也必须一致。

完整的最小配置示例：

```json
{
  "AIRCARFT": "Cessna 172",
  "RA_CHECKBOX": false,
  "LD_CHECKBOX": true,
  "PAGESWITCH": "-",
  "PAGE_1": {},
  "PAGE_2": {}
}
```

### 1.1 顶层字段

| 字段 | 类型 | 含义 |
| --- | --- | --- |
| `AIRCARFT` | string | 当前配置对应的飞机名称。字段名沿用现有工程拼写。 |
| `RA_CHECKBOX` | boolean | 是否勾选主窗口的无线电高度选项 `ckbRA`。 |
| `LD_CHECKBOX` | boolean | 是否勾选主窗口的着陆率选项 `ckbLR`。 |
| `PAGESWITCH` | string | 页面切换按键名称。按下后在 `PAGE_1` 和 `PAGE_2` 之间切换。空字符串表示不设置页面切换键。 |
| `PAGE_1` | object | 第一套按键配置。可以为空对象。 |
| `PAGE_2` | object | 第二套按键配置。可以为空对象。 |

除上述字段外，暂不定义其他顶层字段。未知字段可以忽略，但不能替代必需字段。

`AIRCARFT`、`PAGESWITCH` 即使为空字符串，只要类型正确，仍属于格式合法；是否能匹配当前飞机或当前按键，由后续功能决定。

---

## 2. 页面结构

每个页面是一个 JSON 对象。页面可以为空对象，表示该页面没有任何按键绑定；页面不为空时必须包含 `VARIABLES` 字段。页面也可以只包含 `VARIABLES`，表示该页面没有按键绑定。

```json
{
  "VARIABLES": [],
  "V": [
    {
      "THEN": "vnav = 1"
    }
  ]
}
```

页面中的字段分为两类：

1. `VARIABLES`：变量定义；
2. 其他字段：按键名称及其规则数组。

页面中的按键名称是 JSON 对象的字段名，例如：

```json
"V": [ ... ],
"L CTRL+B": [ ... ],
"NUM 7": [ ... ]
```

按键名称不在配置加载阶段做固定字符串校验，运行时与输入监听器产生的按键名称进行匹配。没有收到对应按键时，该绑定不会触发。

---

## 3. VARIABLES 变量表

`VARIABLES` 是数组。数组中的每个元素必须是一个只包含一个键值对的 JSON 对象：

```json
"VARIABLES": [
  { "vnav": "1231123123" },
  { "hdg": "4567890123" },
  { "gearLight": "9876543210" }
]
```

结构含义为：

```text
变量名 -> InputEvent Hash 字符串
```

### 3.1 变量名

变量名由用户自行定义，后续在 `IF`、`THEN` 和 `ELSE` 中使用。

变量名不需要额外绑定飞机控件名称，也不需要与 InputEvent 的原始名称相同。例如：

```json
{ "vnav": "1231123123" }
```

这里的 `vnav` 只是用户定义的简短名称。

变量名实际出现在表达式或赋值语句中时，必须能够被解析器识别为一个完整名称；不能与空白、运算符、括号、分号或等号混在一起产生歧义。

### 3.2 Hash

Hash 按字符串保存：

```json
{ "vnav": "1231123123" }
```

这样可以避免 JSON 数字类型在处理较大的 `UINT64` 时发生精度问题。

配置加载阶段不主动验证 Hash 是否有效，也不要求预先验证它是否属于当前飞机。

真正执行读取或发送时，程序会依次：

1. 将 Hash 字符串转换为 `quint64`；
2. 调用对应的 SimConnect InputEvent 接口；
3. 根据调用结果继续或停止当前按键行为。

Hash 转换失败、SimConnect 调用失败、返回值不是 `DOUBLE` 或无法得到有效的浮点值，都视为当前执行失败。

### 3.3 重复变量名

变量表按照数组顺序从前往后查找。

如果出现重复变量名，使用第一次出现的定义，后面同名定义忽略：

```json
"VARIABLES": [
  { "vnav": "111" },
  { "vnav": "222" }
]
```

实际使用：

```text
vnav -> 111
```

---

## 4. 按键绑定结构

页面中除 `VARIABLES` 以外的字段都可以作为按键绑定。按键字段的值必须是规则数组：

```json
"V": [
  {
    "THEN": "vnav = 1"
  },
  {
    "IF": "hdg == 0",
    "THEN": "hdg = 1",
    "ELSE": "hdg = 0"
  }
]
```

规则数组按照从前到后的顺序执行。

每条规则支持以下字段：

| 字段 | 是否必须 | 类型 | 含义 |
| --- | --- | --- | --- |
| `IF` | 否 | string | 条件表达式。省略时表示无条件执行 `THEN`。 |
| `THEN` | 是 | string | 条件成立时，或没有 `IF` 时执行的赋值流程。 |
| `ELSE` | 否 | string | 条件不成立时执行的赋值流程。只有存在 `IF` 时才有意义。 |

规则约定：

- `THEN` 必须存在；
- `IF` 可以省略；
- `ELSE` 可以省略；
- `ELSE` 不能单独作为一条完整规则使用；
- `IF`、`THEN`、`ELSE` 的值必须是字符串；
- `IF` 为空字符串时，该规则无效；
- 空的 `THEN` 或 `ELSE` 表示该分支不执行任何发送动作；
- 每条规则中只能使用定义在 `VARIABLES` 中的变量；
- 未知字段不参与规则执行；
- 当前规则出现执行错误时，停止当前按键行为的后续规则，不弹窗、不提示。

### 4.1 无条件执行

没有 `IF` 时，直接执行 `THEN`：

```json
"V": [
  {
    "THEN": "vnav = 1"
  }
]
```

含义：

```text
按下 V -> 向 vnav 对应的 Hash 发送 1.0
```

### 4.2 条件执行

```json
"NUM 7": [
  {
    "IF": "gearLight == 1",
    "THEN": "gearLight = 0",
    "ELSE": "gearLight = 1"
  }
]
```

含义：

```text
按下 NUM 7
-> 读取 gearLight 的最新值
-> 如果值为 1，发送 0
-> 否则发送 1
```

因此，开关切换不需要特殊关键字，使用普通的 `IF`、`THEN`、`ELSE` 即可实现。

### 4.3 多条规则

```json
"L CTRL+B": [
  {
    "IF": "vnav == 1",
    "THEN": "vnav = 0",
    "ELSE": "vnav = 1"
  },
  {
    "THEN": "hdg = 1"
  }
]
```

执行顺序为：

1. 执行第一条规则；
2. 第一条规则完成后，执行第二条规则；
3. 第二条规则中的发送动作使用它执行到该步骤时的值和状态。

如果第一条规则失败，第二条规则不执行。

---

## 5. IF 条件表达式

`IF` 的内容是一个条件表达式字符串。

示例：

```json
"IF": "vnav == 1 && hdg == 0"
```

条件表达式支持：

### 5.1 比较运算符

```text
==
>=
>
<=
<
```

两侧可以是变量名或数字：

```text
vnav == 1
hdg >= 0
gearLight < 1
```

第一版不支持 `!=`。如果需要“不等于”，应通过现有的比较条件组合表达，或在后续语法版本中单独增加。

### 5.2 逻辑运算符

```text
&&
||
```

含义与 C 语言一致：

```text
&& -> 逻辑与
|| -> 逻辑或
```

### 5.3 括号

使用 `(` 和 `)` 修改或明确优先级：

```text
(vnav == 1 || hdg == 0) && gearLight == 1
```

括号必须成对出现。

### 5.4 运算符优先级

从高到低：

```text
1. 括号                     ( )
2. 比较运算符               == >= > <= <
3. 逻辑与                   &&
4. 逻辑或                   ||
```

因此：

```text
vnav == 1 && hdg == 0
```

等价于：

```text
(vnav == 1) && (hdg == 0)
```

而：

```text
vnav == 1 || hdg == 0 && gearLight == 1
```

等价于：

```text
(vnav == 1) || ((hdg == 0) && (gearLight == 1))
```

用户只有在需要改变默认优先级时才需要添加括号。

### 5.5 空白字符

条件表达式中的空格、Tab 和换行仅用于提高可读性，解析时忽略。

以下写法含义相同：

```text
vnav == 1 && hdg == 0
```

```text
vnav==1&&hdg==0
```

推荐用户使用带空格的写法。

### 5.6 短路求值

条件使用 C 语言式短路求值：

```text
A && B
```

如果 `A` 已经为假，则不再读取或计算 `B`。

```text
A || B
```

如果 `A` 已经为真，则不再读取或计算 `B`。

例如：

```text
vnav == 1 || hdg == 0
```

当 `vnav == 1` 成立时，不再查询 `hdg`。

短路求值意味着 IF 中的变量不是一定全部查询；只有实际计算到的比较项才会读取对应的 Hash。

---

## 6. THEN / ELSE 执行表达式

`THEN` 和 `ELSE` 的内容是赋值语句字符串。赋值的实际含义不是修改本地变量，而是向变量对应的 InputEvent Hash 发送一个 `double` 值。

基本格式：

```text
变量名 = 数字
```

示例：

```text
vnav = 1
hdg = 0.5
gearLight = -1.0
```

### 6.1 多条执行语句

多条赋值语句使用分号 `;` 分隔：

```text
vnav = 1; hdg = 0; gearLight = 1
```

执行顺序从左到右。

最后一条语句可以带分号：

```text
vnav = 1; hdg = 0;
```

以下内容暂不支持：

```text
vnav = hdg
vnav = 1 + 2
vnav += 1
vnav == 1
```

右侧必须是可以解析为 `double` 的数字。右侧不是数字时，该执行流程不执行，并按照规则错误处理。

### 6.2 执行失败

执行过程中出现以下情况，都视为当前按键行为失败：

- 变量名没有在 `VARIABLES` 中找到；
- Hash 字符串无法转换为 `quint64`；
- `SimConnect_SetInputEvent` 调用失败；
- 发送值或参数不符合当前只支持 `DOUBLE` 的限制；
- 其他导致当前读取或发送无法完成的错误。

失败处理规则：

```text
停止当前按键行为的后续执行
不执行后面的赋值语句
不执行当前按键规则数组中后面的规则
不弹出错误窗口
不向用户显示错误提示
```

例如：

```json
"V": [
  {
    "THEN": "vnav = 1; invalid = 0; hdg = 1"
  },
  {
    "THEN": "gearLight = 1"
  }
]
```

如果 `invalid` 不存在，当前按键行为在此处停止；`hdg = 1` 和下一条规则都不会执行。

---

## 7. 读取和发送的数据类型

配置规则只支持参数类型为 `DOUBLE` 的 InputEvent。

不支持：

```text
STRING
char[N]
多个非 DOUBLE 参数
```

`IF` 中的变量表示读取对应 Hash 的当前 `double` 值。

`THEN` / `ELSE` 中的变量赋值表示使用 `SimConnect_SetInputEvent` 发送一个 `double` 值，发送长度固定为：

```text
sizeof(double) == 8
```

如果读取到的 InputEvent 不是 `DOUBLE`，当前按键行为停止，不提示用户。

---

## 8. 完整执行模型

### 8.1 按键触发

当输入监听器产生一个按键名称时：

1. 如果按键名称等于 `PAGESWITCH`，优先执行页面切换；
2. 页面切换完成后，不执行同一次按键对应的页面绑定；
3. 如果不是页面切换键，则在当前页面查找同名按键字段；
4. 找到后从规则数组第一条开始执行；
5. 没有找到则忽略该按键。

`PAGESWITCH` 具有最高优先级，即使当前页面中也存在同名按键绑定，页面切换仍优先。

### 8.2 单条规则

有 `IF` 时：

```text
开始计算 IF
  -> 按表达式优先级读取需要的变量最新值
  -> 根据短路规则继续或停止读取
  -> IF 成立，执行 THEN
  -> IF 不成立且存在 ELSE，执行 ELSE
  -> IF 不成立且没有 ELSE，当前规则完成
```

没有 `IF` 时：

```text
直接执行 THEN
```

### 8.3 多条语句和多条规则

所有操作都严格按出现顺序执行：

```text
规则 1
  -> THEN/ELSE 语句 1
  -> THEN/ELSE 语句 2
规则 2
  -> IF 重新读取需要的最新值
  -> THEN/ELSE 语句 1
```

程序不会在按键刚触发时预先读取所有 IF 变量。每一步执行到时，才读取该步骤需要的最新值。

因此，上一步发送完成后，下一步读取的是下一步开始时的值，而不是按键触发瞬间缓存的旧值。

本规则不额外等待模拟器内部状态更新。发送接口调用完成后，立即按配置顺序继续下一步。

### 8.4 重复触发

执行一个按键行为期间，程序处于忙状态。

如果此时再次收到任何按键触发：

```text
忽略这次触发
不排队
不打断当前执行
不重新开始当前执行
```

当前按键行为完整结束或因错误停止后，程序才恢复接收新的按键行为。

---

## 9. 完整示例

下面是一个完整的标准 JSON 配置示例：

```json
{
  "AIRCARFT": "SF50",
  "RA_CHECKBOX": true,
  "LD_CHECKBOX": true,
  "PAGESWITCH": "-",
  "PAGE_1": {
    "VARIABLES": [
      { "vnav": "1231123123" },
      { "hdg": "4567890123" },
      { "gearLight": "9876543210" }
    ],
    "V": [
      {
        "THEN": "vnav = 1"
      }
    ],
    "NUM 7": [
      {
        "IF": "gearLight == 1",
        "THEN": "gearLight = 0",
        "ELSE": "gearLight = 1"
      }
    ],
    "L CTRL+B": [
      {
        "IF": "vnav == 1 && hdg == 0",
        "THEN": "vnav = 0; hdg = 1",
        "ELSE": "vnav = 1; hdg = 0"
      },
      {
        "THEN": "gearLight = 1;"
      }
    ]
  },
  "PAGE_2": {
    "VARIABLES": []
  }
}
```

执行 `L CTRL+B` 时：

1. 读取 `vnav` 的最新值；
2. 如果 `vnav == 1`，再读取 `hdg` 的最新值；
3. 如果 `hdg == 0`，按顺序发送 `vnav = 0`、`hdg = 1`；
4. 否则按顺序发送 `vnav = 1`、`hdg = 0`；
5. 第一条规则完成后执行第二条规则；
6. 第二条规则发送 `gearLight = 1`。

如果第一条规则任意一步失败，第 5、6 步不执行。

---

## 10. 规则错误处理原则

当前配置系统采用静默失败策略：

- JSON 语法错误：配置文件无效，不能正常加载；
- 顶层必需字段缺失或类型错误：配置文件无效；
- 页面结构或变量结构错误：对应页面或配置按无效处理；
- 某条 `IF`、`THEN`、`ELSE` 语法错误：跳过该规则；
- 执行过程中 Hash、读取或发送失败：停止当前按键行为；
- 运行时规则错误不弹窗、不提示用户；
- 一个按键行为的失败不影响其他按键配置本身。

“跳过规则”和“停止当前按键行为”区别如下：

| 情况 | 处理 |
| --- | --- |
| 规则在开始解析时发现 `IF` 或执行字符串格式错误 | 跳过该规则，继续检查规则数组后面的规则 |
| 规则已经开始执行，但变量读取或事件发送失败 | 停止当前按键行为，不再执行后续规则 |

---

## 11. 当前版本限制

当前语法暂不支持：

- `!=` 运算符；
- 算术运算，例如 `+`、`-`、`*`、`/`；
- 变量赋值后再作为另一个表达式变量使用；
- 字符串类型 InputEvent；
- `char[N]` 参数；
- 循环；
- 函数；
- 用户脚本；
- 规则排队；
- 执行中的按键行为被新的按键打断或重启。

这些限制是为了让配置语法保持简单，并确保所有规则都可以明确转换为当前支持的 SimConnect `DOUBLE` InputEvent 读取和发送操作。
